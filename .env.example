# CCMHA Schedule Monitor Configuration

# Email Settings
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-gmail-app-password
RECIPIENT_EMAILS=recipient1@example.com,recipient2@example.com

# SMTP Settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Scraping Settings
VENUE_FILTER=Amherst Stadium
DAYS_AHEAD=14
CHANGE_MONITOR_DAYS=7

# Output Directory (inside Docker container)
OUTPUT_DIR=/data

# Test Mode (set to true to test without sending emails)
TEST_MODE=false
